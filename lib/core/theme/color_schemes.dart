import 'package:flutter/material.dart';

const Color primaryColor = Color(0xffDE2E0B);
const Color backgroundLight = white;
// const Color backgroundDark = Color(0xff232323);
const Color backgroundDark = Color(0xFF121212);

// Default Colors
const Color white = Colors.white;
const Color black = Colors.black;
const Color lightGray = Color(0xffc6c3c3);
const Color red = Colors.red;
const yellow = Colors.yellow;


const ColorScheme colorSchemeLight = ColorScheme.light(
  primary: primaryColor,
);

const ColorScheme colorSchemeDark = ColorScheme.dark(
  primary: primaryColor
);